# The release toolchain (@semantic-release/npm 13) requires Node 22.14+, but the
# package itself supports Node 20 and CI tests there. Without this, a stricter
# pnpm default would refuse to install dev dependencies on the Node 20 job.
engine-strict=false
